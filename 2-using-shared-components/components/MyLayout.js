import Header from './Header'

const layoutStyle = {
  margin: '1.5rem',
  padding: '1.25rem',
  border: '1px solid #DDD'
}

const Layout = props => (
  <div style={layoutStyle}>
    <header />
  </div>
)

export default Layout